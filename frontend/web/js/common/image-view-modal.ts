import {Modal} from './../common/modal';

export class ImageViewModal extends Modal {
    constructor(root : HTMLElement) {
        super(root);
    }

    decorate() {
        super.decorate();
    }
}   